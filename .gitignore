# ===================== Python =====================
__pycache__/
*.py[cod]
*$py.class
*.so
.python-version

# Environment / secrets
.env
.env.*
!.env.example

# ===================== Virtualenv =====================
venv/
.venv/
env/
ENV/

# ===================== Test / tooling caches =====================
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
.tox/
.nox/

# ===================== IDE / OS =====================
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ===================== Runtime =====================
*.log
*.err

# ===================== Data (regenerable, too large for GitHub) =====================
/data/
data/*.npz

# ===================== Models =====================
# Trained artifacts under models/ ARE committed: a fresh clone (and the
# fly.io build from the GitHub source) must boot in ML mode, not rules mode.
# Everything else stays out of git — data/ is regenerable from the engine.
